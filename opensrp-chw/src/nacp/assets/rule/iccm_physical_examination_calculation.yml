---
name: step1_is_malaria_suspect
description: is_malaria_suspect calculation
priority: 1
condition: "true"
actions:
  - "calculation = !step1_physical_examination.isEmpty() && (global_is_malaria_suspect || !step1_physical_examination.contains('none'))"
